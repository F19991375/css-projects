<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animated button</title>
    <link rel="stylesheet" href="animated-button.css">
</head>
<body>
    <div class = "container">
        <a href="#" style = "--clr: #22ffed; --i: 0">
            <span>Buton</span>
        </a>
        <a href="#" style = "--clr: #00ccff; --i: 1" >
            <span>Button</span>
        </a>
        <a href="#" style = "--clr: #22e622; --i: 3">
            <span>Button</span>
        </a>
    </div>
    
</body>
</html>


body{
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: #abafc5;
    font-family: sans-serif;
    overflow: hidden;
}

.container{
    display: flex;
    gap: 20px;
}

a{
    position: relative;
    display: inline-block;
    padding: 10px 30px;
    color: rgb(109, 6, 6);
    text-decoration: none;
    text-transform: uppercase;
    font-size: 18px;
    font-weight: 200;
    overflow: hidden;
    border: 2px solid var(--clr);
    border-radius: 5px;
}

a:before{
    content: "";
    position: absolute;
    top: -2px;
    bottom: -2px;
    left: -2px;
    right: -2px;
    background: linear-gradient(90deg, transparent, var(--clr), transparent);
    z-index: -1;
    animation: flow 2s linear infinite;
}

a:hover{
    background-color: var(--clr);
    box-shadow: 0 9px 15px var(--clr);

}


@keyframes flow{
    0%{
        transform: translate(-100px);
    }

    100%{
        transform: translateX(100%);
    }
}
